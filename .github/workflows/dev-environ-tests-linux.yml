name: dev-environ-tests-linux
on:
  push:
    branches:
      - dev
jobs:
  unit-tests-linux-misc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Configure agent
        env:
          ACM_SECRET: ${{ secrets.ACM_REPO }}
          ACM_SECOND: ${{ secrets.ACM_REPO_SECOND }}
        run: |
          az extension add --name resource-graph
          echo "Current working directory is: "
          pwd
          mkdir /home/runner/keys
          mkdir /home/runner/keys/starter
          mkdir /home/runner/acmconfig/
          echo "$ACM_SECRET" > /home/runner/acmconfig/keys.yaml
          sudo mkdir /var/log/acm
          sudo chown -R runner:runner /var/log/acm
          sudo mkdir /opt/acm
          sudo chown -R runner:runner /opt/acm
          sudo mkdir /usr/acm
          sudo mkdir /usr/acm/keys
          sudo mkdir /usr/acm/keys/adminAccounts
          sudo chown -R runner:runner /usr/acm
          echo "$ACM_SECOND" > /usr/acm/keys/adminAccounts/keys.yaml
          sudo chown -R runner:runner /usr/acm
          echo 'new------------------'
          git --version
          sudo apt install -y dos2unix
          ## Install Terraform
          cd /home/runner
          mkdir terraform-download
          cd terraform-download
          wget https://releases.hashicorp.com/terraform/0.12.24/terraform_0.12.24_linux_amd64.zip
          unzip terraform_0.12.24_linux_amd64.zip
          #Move the terraform binary into a folder that is listed as part of the PATH variable.  
          mv terraform /usr/local/bin/
          echo 'and---------------------------------'
          cd /home/runner
          sudo apt -y install ansible
          aws --version
          pip3 install requests
          pip3 install pyyaml
          pip3 install IPy
          python3 --version
          python --version
          #Cause the ansible configuration to be owned by the agent user so that pipelines can change things like hosts file, etc.  
          sudo chown -R runner:runner /opt/pipx_bin/ansible
          echo 'then--------------------------------'
          sudo apt update && sudo apt upgrade
          sudo apt clean
      - shell: bash
        name: Run setup on
        run: |
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++'
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          python AgileCloudManager/app/acm.py setup on sourceRepo=https://github.com/AgileCloudInstitute/acm-config-projects-cf.git
      - shell: bash
        name: Run test_command_builder
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run unit test"
          python -m unittest AgileCloudManager.unitTests.test_command_builder
      - shell: bash
        name: Run test_workflow_and_changes
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run unit test"
          python -m unittest AgileCloudManager.unitTests.test_workflow_and_changes
      - shell: bash
        name: Run test_controller_custom.py
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run unit test"
          python -m unittest AgileCloudManager.unitTests.test_controller_custom
      - shell: bash
        name: Delete Secrets
        run: |
          rm /home/runner/acmconfig/keys.yaml
  unit-tests-linux-cloudformation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Configure agent
        env:
          ACM_SECRET: ${{ secrets.ACM_REPO }}
          ACM_SECOND: ${{ secrets.ACM_REPO_SECOND }}
        run: |
          az extension add --name resource-graph
          echo "Current working directory is: "
          pwd
          mkdir /home/runner/keys
          mkdir /home/runner/keys/starter
          mkdir /home/runner/acmconfig/
          echo "$ACM_SECRET" > /home/runner/acmconfig/keys.yaml
          sudo mkdir /var/log/acm
          sudo chown -R runner:runner /var/log/acm
          sudo mkdir /opt/acm
          sudo chown -R runner:runner /opt/acm
          sudo mkdir /usr/acm
          sudo mkdir /usr/acm/keys
          sudo mkdir /usr/acm/keys/adminAccounts
          sudo chown -R runner:runner /usr/acm
          echo "$ACM_SECOND" > /usr/acm/keys/adminAccounts/keys.yaml
          sudo chown -R runner:runner /usr/acm
          echo 'new------------------'
          git --version
          sudo apt install -y dos2unix
          ## Install Terraform
          cd /home/runner
          mkdir terraform-download
          cd terraform-download
          wget https://releases.hashicorp.com/terraform/0.12.24/terraform_0.12.24_linux_amd64.zip
          unzip terraform_0.12.24_linux_amd64.zip
          #Move the terraform binary into a folder that is listed as part of the PATH variable.  
          mv terraform /usr/local/bin/
          echo 'and---------------------------------'
          cd /home/runner
          sudo apt -y install ansible
          aws --version
          pip3 install requests
          pip3 install pyyaml
          pip3 install IPy
          python3 --version
          python --version
          #Cause the ansible configuration to be owned by the agent user so that pipelines can change things like hosts file, etc.  
          sudo chown -R runner:runner /opt/pipx_bin/ansible
          echo 'then--------------------------------'
          sudo apt update && sudo apt upgrade
          sudo apt clean
      - shell: bash
        name: Run setup on
        run: |
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++'
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          python AgileCloudManager/app/acm.py setup on sourceRepo=https://github.com/AgileCloudInstitute/acm-config-projects-cf.git
      - shell: bash
        name: Run test_controller_cf
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run unit test"
          python -m unittest AgileCloudManager.unitTests.test_controller_cf
      - shell: bash
        name: Delete Secrets
        run: |
          rm /home/runner/acmconfig/keys.yaml
  unit-tests-linux-terraform-packer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Configure agent
        env:
          ACM_SECRET: ${{ secrets.ACM_REPO }}
          ACM_SECOND: ${{ secrets.ACM_REPO_SECOND }}
        run: |
          az extension add --name resource-graph
          echo "Current working directory is: "
          pwd
          mkdir /home/runner/keys
          mkdir /home/runner/keys/starter
          mkdir /home/runner/acmconfig/
          echo "$ACM_SECRET" > /home/runner/acmconfig/keys.yaml
          sudo mkdir /var/log/acm
          sudo chown -R runner:runner /var/log/acm
          sudo mkdir /opt/acm
          sudo chown -R runner:runner /opt/acm
          sudo mkdir /usr/acm
          sudo mkdir /usr/acm/keys
          sudo mkdir /usr/acm/keys/adminAccounts
          sudo chown -R runner:runner /usr/acm
          echo "$ACM_SECOND" > /usr/acm/keys/adminAccounts/keys.yaml
          sudo chown -R runner:runner /usr/acm
          echo 'new------------------'
          git --version
          sudo apt install -y dos2unix
          ## Install Terraform
          cd /home/runner
          mkdir terraform-download
          cd terraform-download
          wget https://releases.hashicorp.com/terraform/0.12.24/terraform_0.12.24_linux_amd64.zip
          unzip terraform_0.12.24_linux_amd64.zip
          #Move the terraform binary into a folder that is listed as part of the PATH variable.  
          mv terraform /usr/local/bin/
          echo 'and---------------------------------'
          cd /home/runner
          sudo apt -y install ansible
          aws --version
          pip3 install requests
          pip3 install pyyaml
          pip3 install IPy
          python3 --version
          python --version
          #Cause the ansible configuration to be owned by the agent user so that pipelines can change things like hosts file, etc.  
          sudo chown -R runner:runner /opt/pipx_bin/ansible
          echo 'then--------------------------------'
          sudo apt update && sudo apt upgrade
          sudo apt clean
      - shell: bash
        name: Run setup on
        run: |
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++'
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          python AgileCloudManager/app/acm.py setup on sourceRepo=https://github.com/AgileCloudInstitute/acm-config-projects-cf.git
      - shell: bash
        name: Run test_controller_terraform_and_packer_one
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run unit test"
          python -m unittest AgileCloudManager.unitTests.test_controller_terraform_and_packer_one
      - shell: bash
        name: Run test_controller_terraform_and_packer_two
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run unit test"
          python -m unittest AgileCloudManager.unitTests.test_controller_terraform_and_packer_two
      - shell: bash
        name: Delete Secrets
        run: |
          rm /home/runner/acmconfig/keys.yaml
  unit-tests-linux-arm-template:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Configure agent
        env:
          ACM_SECRET: ${{ secrets.ACM_REPO }}
          ACM_SECOND: ${{ secrets.ACM_REPO_SECOND }}
        run: |
          az extension add --name resource-graph
          echo "Current working directory is: "
          pwd
          mkdir /home/runner/keys
          mkdir /home/runner/keys/starter
          mkdir /home/runner/acmconfig/
          echo "$ACM_SECRET" > /home/runner/acmconfig/keys.yaml
          sudo mkdir /var/log/acm
          sudo chown -R runner:runner /var/log/acm
          sudo mkdir /opt/acm
          sudo chown -R runner:runner /opt/acm
          sudo mkdir /usr/acm
          sudo mkdir /usr/acm/keys
          sudo mkdir /usr/acm/keys/adminAccounts
          sudo chown -R runner:runner /usr/acm
          echo "$ACM_SECOND" > /usr/acm/keys/adminAccounts/keys.yaml
          sudo chown -R runner:runner /usr/acm
          echo 'new------------------'
          git --version
          sudo apt install -y dos2unix
          ## Install Terraform
          cd /home/runner
          mkdir terraform-download
          cd terraform-download
          wget https://releases.hashicorp.com/terraform/0.12.24/terraform_0.12.24_linux_amd64.zip
          unzip terraform_0.12.24_linux_amd64.zip
          #Move the terraform binary into a folder that is listed as part of the PATH variable.  
          mv terraform /usr/local/bin/
          echo 'and---------------------------------'
          cd /home/runner
          sudo apt -y install ansible
          aws --version
          pip3 install requests
          pip3 install pyyaml
          pip3 install IPy
          python3 --version
          python --version
          #Cause the ansible configuration to be owned by the agent user so that pipelines can change things like hosts file, etc.  
          sudo chown -R runner:runner /opt/pipx_bin/ansible
          echo 'then--------------------------------'
          sudo apt update && sudo apt upgrade
          sudo apt clean
      - shell: bash
        name: Run setup on
        run: |
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++'
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          python AgileCloudManager/app/acm.py setup on sourceRepo=https://github.com/AgileCloudInstitute/acm-config-projects-cf.git
      - shell: bash
        name: Run test_controller_arm
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run unit test"
          python -m unittest AgileCloudManager.unitTests.test_controller_arm
      - shell: bash
        name: Delete Secrets
        run: |
          rm /home/runner/acmconfig/keys.yaml
  e2e-tests-linux-arm:
    needs: [unit-tests-linux-misc, unit-tests-linux-cloudformation, unit-tests-linux-terraform-packer, unit-tests-linux-arm-template]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Configure agent
        env:
          ACM_SECRET: ${{ secrets.ACM_REPO }}
          ACM_SECOND: ${{ secrets.ACM_REPO_SECOND }}
        run: |
          az extension add --name resource-graph
          echo "Current working directory is: "
          pwd
          mkdir /home/runner/keys
          mkdir /home/runner/keys/starter
          mkdir /home/runner/acmconfig/
          echo "$ACM_SECRET" > /home/runner/acmconfig/keys.yaml
          sudo mkdir /var/log/acm
          sudo chown -R runner:runner /var/log/acm
          sudo mkdir /opt/acm
          sudo chown -R runner:runner /opt/acm
          sudo mkdir /usr/acm
          sudo mkdir /usr/acm/keys
          sudo mkdir /usr/acm/keys/adminAccounts
          sudo chown -R runner:runner /usr/acm
          echo "$ACM_SECOND" > /usr/acm/keys/adminAccounts/keys.yaml
          sudo chown -R runner:runner /usr/acm
          echo 'new------------------'
          git --version
          sudo apt install -y dos2unix
          ## Install Terraform
          cd /home/runner
          mkdir terraform-download
          cd terraform-download
          wget https://releases.hashicorp.com/terraform/0.12.24/terraform_0.12.24_linux_amd64.zip
          unzip terraform_0.12.24_linux_amd64.zip
          #Move the terraform binary into a folder that is listed as part of the PATH variable.  
          mv terraform /usr/local/bin/
          echo 'and---------------------------------'
          cd /home/runner
          sudo apt -y install ansible
          aws --version
          pip3 install requests
          pip3 install pyyaml
          pip3 install IPy
          python3 --version
          python --version
          #Cause the ansible configuration to be owned by the agent user so that pipelines can change things like hosts file, etc.  
          sudo chown -R runner:runner /opt/pipx_bin/ansible
          echo 'then--------------------------------'
          sudo apt update && sudo apt upgrade
          sudo apt clean
      - shell: bash
        name: Run setup on
        run: |
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++'
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          python AgileCloudManager/app/acm.py setup on sourceRepo=https://github.com/AgileCloudInstitute/acm-config-projects-arm.git
      - shell: bash
        name: Run platform on
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run e2e test"
          python AgileCloudManager/app/acm.py platform on
      - shell: bash
        name: Run platform off
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run e2e test"
          python AgileCloudManager/app/acm.py platform off
      - shell: bash
        name: Delete Secrets
        run: |
          rm /home/runner/acmconfig/keys.yaml
  e2e-tests-linux-cloudformation:
    needs: [unit-tests-linux-misc, unit-tests-linux-cloudformation, unit-tests-linux-terraform-packer, unit-tests-linux-arm-template]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Configure agent
        env:
          ACM_SECRET: ${{ secrets.ACM_REPO }}
          ACM_SECOND: ${{ secrets.ACM_REPO_SECOND }}
        run: |
          az extension add --name resource-graph
          echo "Current working directory is: "
          pwd
          mkdir /home/runner/keys
          mkdir /home/runner/keys/starter
          mkdir /home/runner/acmconfig/
          echo "$ACM_SECRET" > /home/runner/acmconfig/keys.yaml
          sudo mkdir /var/log/acm
          sudo chown -R runner:runner /var/log/acm
          sudo mkdir /opt/acm
          sudo chown -R runner:runner /opt/acm
          sudo mkdir /usr/acm
          sudo mkdir /usr/acm/keys
          sudo mkdir /usr/acm/keys/adminAccounts
          sudo chown -R runner:runner /usr/acm
          echo "$ACM_SECOND" > /usr/acm/keys/adminAccounts/keys.yaml
          sudo chown -R runner:runner /usr/acm
          echo 'new------------------'
          git --version
          sudo apt install -y dos2unix
          ## Install Terraform
          cd /home/runner
          mkdir terraform-download
          cd terraform-download
          wget https://releases.hashicorp.com/terraform/0.12.24/terraform_0.12.24_linux_amd64.zip
          unzip terraform_0.12.24_linux_amd64.zip
          #Move the terraform binary into a folder that is listed as part of the PATH variable.  
          mv terraform /usr/local/bin/
          echo 'and---------------------------------'
          cd /home/runner
          sudo apt -y install ansible
          aws --version
          pip3 install requests
          pip3 install pyyaml
          pip3 install IPy
          python3 --version
          python --version
          #Cause the ansible configuration to be owned by the agent user so that pipelines can change things like hosts file, etc.  
          sudo chown -R runner:runner /opt/pipx_bin/ansible
          echo 'then--------------------------------'
          sudo apt update && sudo apt upgrade
          sudo apt clean
      - shell: bash
        name: Run setup on
        run: |
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++'
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          python AgileCloudManager/app/acm.py setup on sourceRepo=https://github.com/AgileCloudInstitute/acm-config-projects-cf.git
      - shell: bash
        name: Run platform on
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run e2e test"
          python AgileCloudManager/app/acm.py platform on
      - shell: bash
        name: Run platform off
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run e2e test"
          python AgileCloudManager/app/acm.py platform off
      - shell: bash
        name: Delete Secrets
        run: |
          rm /home/runner/acmconfig/keys.yaml
  e2e-tests-linux-custom:
    needs: [unit-tests-linux-misc, unit-tests-linux-cloudformation, unit-tests-linux-terraform-packer, unit-tests-linux-arm-template]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Configure agent
        env:
          ACM_SECRET: ${{ secrets.ACM_REPO }}
          ACM_SECOND: ${{ secrets.ACM_REPO_SECOND }}
        run: |
          az extension add --name resource-graph
          echo "Current working directory is: "
          pwd
          mkdir /home/runner/keys
          mkdir /home/runner/keys/starter
          mkdir /home/runner/acmconfig/
          echo "$ACM_SECRET" > /home/runner/acmconfig/keys.yaml
          sudo mkdir /var/log/acm
          sudo chown -R runner:runner /var/log/acm
          sudo mkdir /opt/acm
          sudo chown -R runner:runner /opt/acm
          sudo mkdir /usr/acm
          sudo mkdir /usr/acm/keys
          sudo mkdir /usr/acm/keys/adminAccounts
          sudo chown -R runner:runner /usr/acm
          echo "$ACM_SECOND" > /usr/acm/keys/adminAccounts/keys.yaml
          sudo chown -R runner:runner /usr/acm
          echo 'new------------------'
          git --version
          sudo apt install -y dos2unix
          ## Install Terraform
          cd /home/runner
          mkdir terraform-download
          cd terraform-download
          wget https://releases.hashicorp.com/terraform/0.12.24/terraform_0.12.24_linux_amd64.zip
          unzip terraform_0.12.24_linux_amd64.zip
          #Move the terraform binary into a folder that is listed as part of the PATH variable.  
          mv terraform /usr/local/bin/
          echo 'and---------------------------------'
          cd /home/runner
          sudo apt -y install ansible
          aws --version
          pip3 install requests
          pip3 install pyyaml
          pip3 install IPy
          python3 --version
          python --version
          #Cause the ansible configuration to be owned by the agent user so that pipelines can change things like hosts file, etc.  
          sudo chown -R runner:runner /opt/pipx_bin/ansible
          echo 'then--------------------------------'
          sudo apt update && sudo apt upgrade
          sudo apt clean
      - shell: bash
        name: Run setup on
        run: |
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++'
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          python AgileCloudManager/app/acm.py setup on sourceRepo=https://github.com/AgileCloudInstitute/acm-config-custom.git
      - shell: bash
        name: Run platform on
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run e2e test"
          python AgileCloudManager/app/acm.py platform on
      - shell: bash
        name: Run platform off
        run: |
          echo "................about to pwd"
          pwd
          echo "................about to list contents"
          ls -al
          echo "................about to cd /home/runner/work/AgileCloudManager/ "
          cd /home/runner/work/AgileCloudManager/
          echo "Current working directory is: "
          pwd
          echo 'Contents are: --------------------------------------------------'
          ls -al
          echo ".................about to run e2e test"
          python AgileCloudManager/app/acm.py platform off
      - shell: bash
        name: Delete Secrets
        run: |
          rm /home/runner/acmconfig/keys.yaml
  e2e-tests-linux-terraform-packer:
    needs: [unit-tests-linux-misc, unit-tests-linux-cloudformation, unit-tests-linux-terraform-packer, unit-tests-linux-arm-template]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Configure agent
        env:
          ACM_SECRET: ${{ secrets.ACM_REPO }}
          ACM_SECOND: ${{ secrets.ACM_REPO_SECOND }}
          ACM_VAR_ONE: /home/runner/acmconfig/keys.yaml
          ACM_VAR_TWO: /usr/acm/keys/adminAccounts/keys.yaml
        run: |
          az extension add --name resource-graph
          echo "Current working directory is: "
          pwd
          mkdir /home/runner/keys
          mkdir /home/runner/keys/starter
          mkdir /home/runner/acmconfig/
          echo "$ACM_SECRET" > /home/runner/acmconfig/keys.yaml
          #export ACM_VAR_ONE=/home/runner/acmconfig/keys.yaml
          #sudo echo "export ACM_VAR_ONE=/home/runner/acmconfig/keys.yaml" >> /etc/environment
          #sudo echo "export ACM_VAR_ONE=/home/runner/acmconfig/keys.yaml" >> /etc/bashrc
          #sudo echo "export ACM_VAR_ONE=/home/runner/acmconfig/keys.yaml" >> /etc/profile
          sudo mkdir /var/log/acm
          sudo chown -R runner:runner /var/log/acm
          sudo mkdir /opt/acm
          sudo chown -R runner:runner /opt/acm
          sudo mkdir /usr/acm
          sudo mkdir /usr/acm/keys
          sudo mkdir /usr/acm/keys/adminAccounts
          sudo chown -R runner:runner /usr/acm 
          echo "$ACM_SECOND" > /usr/acm/keys/adminAccounts/keys.yaml
          #export ACM_VAR_TWO=/usr/acm/keys/adminAccounts/keys.yaml
          #sudo echo "export ACM_VAR_TWO=/usr/acm/keys/adminAccounts/keys.yaml" >> /etc/environment
          #sudo echo "export ACM_VAR_TWO=/usr/acm/keys/adminAccounts/keys.yaml" >> /etc/bashrc
          #sudo echo "export ACM_VAR_TWO=/usr/acm/keys/adminAccounts/keys.yaml" >> /etc/profile
          sudo chown -R runner:runner /usr/acm
          echo 'new------------------'
          git --version
          sudo apt install -y dos2unix
          ## Install Terraform
          cd /home/runner
          mkdir terraform-download
          cd terraform-download
          wget https://releases.hashicorp.com/terraform/0.12.24/terraform_0.12.24_linux_amd64.zip
          unzip terraform_0.12.24_linux_amd64.zip
          #Move the terraform binary into a folder that is listed as part of the PATH variable.  
          mv terraform /usr/local/bin/
          echo 'and---------------------------------'
          cd /home/runner
          sudo apt -y install ansible
          aws --version
          pip3 install requests
          pip3 install pyyaml
          pip3 install IPy
          python3 --version
          python --version
          #Cause the ansible configuration to be owned by the agent user so that pipelines can change things like hosts file, etc.  
          sudo chown -R runner:runner /opt/pipx_bin/ansible
          echo 'then--------------------------------'
          sudo apt update && sudo apt upgrade
          sudo apt clean
      - shell: bash
        name: Run setup on
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py setup on sourceRepo=https://github.com/AgileCloudInstitute/acm-config-terraform-packer.git
      - shell: bash
        name: Run tfbackend serviceType on
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py serviceType on systemName=tfbackend serviceType=tfBackend
      - shell: bash
        name: Run tfbackend serviceType off
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py serviceType off systemName=tfbackend serviceType=tfBackend
      - shell: bash
        name: Run tfbackend serviceInstance on
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py serviceInstance on systemName=tfbackend serviceType=tfBackend serviceInstance=adminAccounts
      - shell: bash
        name: Run tfbackend serviceInstance off
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py serviceInstance off systemName=tfbackend serviceType=tfBackend serviceInstance=adminAccounts
      - shell: bash
        name: Run tfbackend services on
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py services on systemName=tfbackend
      - shell: bash
        name: Run admin foundation on
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py foundation on systemName=admin
      - shell: bash
        name: Run admin services on
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py services on systemName=admin
      - shell: bash
        name: Run agents foundation on
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py foundation on systemName=agents
      - shell: bash
        name: Run agents services on
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py services on systemName=agents
      - shell: bash
        name: Run agents services off
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py services off systemName=agents
      - shell: bash
        name: Run agents foundation off
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py foundation off systemName=agents
      - shell: bash
        name: Run admin services off
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py services off systemName=admin
      - shell: bash
        name: Run admin foundation off
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py foundation off systemName=admin
      - shell: bash
        name: Run tfbackend services off
        run: |
          cd /home/runner/work/AgileCloudManager/
          python AgileCloudManager/app/acm.py services off systemName=tfbackend
      - shell: bash
        name: Delete Secrets
        run: |
          rm /home/runner/acmconfig/keys.yaml
  push-to-test-branch:
    needs: [e2e-tests-linux-arm, e2e-tests-linux-cloudformation, e2e-tests-linux-custom, e2e-tests-linux-terraform-packer ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
        name: Push changes to test branch
        env:
          GIT_PAT: ${{ secrets.HUSH }}
        run: |
          repoWithToken="https://"$GIT_PAT"@github.com/AgileCloudInstitute/AgileCloudManager.git"
          git config --global user.email "Agile.Cloud.Institute@GreenRiverIT.com"
          git config --global user.name "AgileCloudInstitute"
          git init
          git remote set-url origin $repoWithToken
          git branch -M test
          git add --all
          git push --force -u origin test